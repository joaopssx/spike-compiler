# Internals

Internals importantes ja presentes ou planejados:

- `SourceManager` para carregar fonte e acessar linhas
- `DiagnosticEngine` para diagnosticos ao usuario
- `CompilationSession` para estado compartilhado da compilacao
- `PhaseProfiler` para `--time` e `--trace`
- parsing modular da CLI e despacho por comando
