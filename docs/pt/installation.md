# Instalacao

## Linux e macOS

```bash
cmake -S . -B build
cmake --build build
```

## Windows

```bash
cmake -S . -B build
cmake --build build --config Release
```

Use `spike doctor` para verificar o ambiente.
