# Optimization

Optimization work is planned after the IR layer is stable.

Priority passes:

- constant folding
- constant propagation
- dead code analysis
- validation passes

Current status:

- `constant folding` is already wired into the pipeline before IR/code generation
