# Language Reference

Current MVP constructs:

- `algoritmo`, `var`, `inicio`, `fimalgoritmo`
- `inteiro`, `real`
- assignment with `<-`
- `escreva(...)`, `leia(...)`
- `se / senao / fimse`
- `enquanto / faca / fimenquanto`
