# Internals

Important internals already present or planned:

- `SourceManager` for source loading and line access
- `DiagnosticEngine` for user-facing diagnostics
- `CompilationSession` for shared compilation state
- `PhaseProfiler` for `--time` and tracing support
- modular CLI parsing and dispatch
