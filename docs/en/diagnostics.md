# Diagnostics

Spike diagnostics aim to become compiler-grade.

Current direction:

- source-aware messages
- file, line and column info
- caret rendering
- trace output per phase

Planned additions include `warning`, `note` and `help` levels with recovery.
