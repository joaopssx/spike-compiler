# Changelog

## 0.1.0 - 2026-03-31

### Adicionado

- Estrutura inicial do projeto com `CMake`.
- Executavel `spike` com comando `tokens`.
- Sistema simples de leitura de arquivo.
- Definicao de `Token`, `TokenType` e `Lexer`.
- Lexer inicial para palavras-chave, operadores, simbolos, identificadores, numeros e strings.
- Documentacao do projeto em `docs/`.
