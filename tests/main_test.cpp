// Placeholder test runner for the Spike Compiler.
// Real unit tests (lexer, parser, codegen, etc.) will be added here later.
// For now this just prints a success message so CTest has something to run.

#include <iostream>

int main() {
    std::cout << "all tests passed" << std::endl;
    return 0;
}
